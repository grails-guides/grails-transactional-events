databaseChangeLog = {
    // GORM auto-generates the schema in dev; this file is intentionally empty.
    // Add explicit Liquibase changesets here when promoting to environments
    // that require versioned migrations.
}
